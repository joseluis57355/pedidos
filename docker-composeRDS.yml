version: '3.8'
services:
  spring-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=
      - DB_PORT=5432
      - DB_NAME=orders
      - DB_USERNAME=postgres
      - DB_PASSWORD=
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
